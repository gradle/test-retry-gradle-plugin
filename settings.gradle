plugins {
    id "com.gradle.enterprise" version "3.1"
}
rootProject.name = 'test-retry-plugin'

include 'plugin'

['junit5'].each { sample ->
    include "sample-$sample"
    project(":sample-$sample").projectDir = new File(rootProject.projectDir, "samples/sample-$sample")
}
